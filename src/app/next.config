/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // Catches potential problems during development
  swcMinify: true,       // Faster, modern minification for production

  images: {
    // Add domains for Next.js <Image> optimization
    domains: ['localhost', 'example.com'],
  },
  
  // Uncomment if you need API rewrites in the future:
  /*
  async rewrites() {
    return [
      {
        source: '/api/:path*',
        destination: 'https://your-api-domain.com/:path*',
      },
    ];
  },
  */
};

module.exports = nextConfig;
